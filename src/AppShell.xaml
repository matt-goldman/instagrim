<Shell
    x:Class="instagrim.AppShell"
    xmlns:local="clr-namespace:instagrim"
    NavBarIsVisible="False"
    Title="instagrim">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>
